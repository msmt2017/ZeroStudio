git init
git branch -M ZeroStudio-ktlsp
git remote add origin https://github.com/msmt2017/ZeroStudio.git
git add .
git commit -m "update：1.新增kotlin lsp：目前仅合并到ZeroStudio且目前构建正常。2.ktlsp目前需要解决maven libs 冲突问题，再然后才能将lsp-api接入ktlsp"
#git pull origin ZeroStudio
git push  origin ZeroStudio-ktlsp
#git pull origin ZeroStudio --rebase
